<UserControl x:Class="DeliveryApp.Views.DeliveryListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeliveryApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Lista wszystkich przesyłek" Style="{StaticResource TitleTextBlockStyle}" />

        <ListView ItemsSource="{Binding Deliveries}"  Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nadawca" DisplayMemberBinding="{Binding SenderAddress}"/>
                    <GridViewColumn Header="Odbiorca" DisplayMemberBinding="{Binding ReceiverAddress}"/>
                    <GridViewColumn Header="Typ dostawy" DisplayMemberBinding="{Binding DeliveryType}"/>
                    <GridViewColumn Header="Waga" DisplayMemberBinding="{Binding Weight, StringFormat={}{0} kg}"/>
                    <GridViewColumn Header="Szerokość" DisplayMemberBinding="{Binding Width, StringFormat={}{0} cm}"/>
                    <GridViewColumn Header="Długość" DisplayMemberBinding="{Binding Length, StringFormat={}{0} cm}"/>
                    <GridViewColumn Header="Wysokość" DisplayMemberBinding="{Binding Height, StringFormat={}{0} cm}"/>
                    <GridViewColumn Header="Data rejestracji" DisplayMemberBinding="{Binding RegistrationDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
